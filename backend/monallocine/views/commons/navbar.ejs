<nav class="navbar navbar-default">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand visible-xs" href="#">Mon Allociné</a>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="navbar-collapse-1">
      <ul class="nav navbar-nav">
        <li<% if (!menu) { %> class="active"<% } %>><a href="/">Actuellement</a></li>
        <li<% if (menu === 'prochainement') { %> class="active"<% } %>><a href="/prochainement">Prochainement</a></li>
        <li<% if (menu === 'populaires') { %> class="active"<% } %>><a href="/populaires">Populaires</a></li>
        <li<% if (menu === 'favoris') { %> class="active"<% } %>><a href="/favoris" style="position: relative;">Mes films favoris <span class="badge<% if (favorites.length > 0) { %> badgeFull<% } %>" id="badge"><%= favorites.length %></span></a>
        </li>
      </ul>
      <!--
      <form class="navbar-form navbar-right">
        <div class="form-group">
          <input type="text" class="form-control" placeholder="Rechercher un film ici...">
        </div>
        <button type="submit" class="btn btn-default">Rechercher</button>
      </form>
      -->
    </div><!-- /.navbar-collapse -->
</nav>